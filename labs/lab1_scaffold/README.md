# Lab 1: Scaffold & First Tests (Module 6)

Create `my-sensor-toolkit` in this directory.

```bash
cd labs/lab1_scaffold
mkdir my-sensor-toolkit && cd my-sensor-toolkit
git init -b main
claude   # Start Claude Code
```

**Goal:** Scaffold a Python project from scratch with validators, cleaners, analyzers, and tests.

**Sample data:** `../../src/data/sensor_readings_1000.csv`
