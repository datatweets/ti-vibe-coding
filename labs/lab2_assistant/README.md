# Lab 2: Build a Mini Assistant Loop (Module 12)

Work in your `my-sensor-toolkit` project from Lab 1.

```bash
cd labs/lab1_scaffold/my-sensor-toolkit
claude
```

**Goal:** Observe repo → create subagent → plan feature → implement → test → quality gate → PR.
